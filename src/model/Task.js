export default class Task {
    constructor(taskID, dateTime, qualNum, unqualNum) {
        this.taskID = taskID;
        this.dateTime = dateTime;
        this.qualNum = qualNum;
        this.unqualNum = unqualNum;
    }
}